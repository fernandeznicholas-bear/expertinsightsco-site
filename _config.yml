# GitHub Pages / Jekyll config for expertinsightsco.com
#
# The About page is kept in the repo but NOT published — it carries the
# founders' names, and we're holding on whether to attach them publicly.
# To make it public again: delete the "- about.html" line below and push.
exclude:
  - about.html
  - README.md
  - _config.yml
